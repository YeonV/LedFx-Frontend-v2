diff --git a/scripts/postbuild.js b/scripts/postbuild.js
index 329bf55..21a701f 100644
--- a/scripts/postbuild.js
+++ b/scripts/postbuild.js
@@ -11,14 +11,17 @@ import { fileURLToPath } from 'url'
 const __dirname = path.dirname(fileURLToPath(import.meta.url))
 const projectRoot = path.resolve(__dirname, '..')

+// In this environment, the frontend is at /app
+const frontendRoot = '/app'
+
 const copies = [
   {
     src: path.join(projectRoot, 'dist/yz-audio-visualiser.js'),
-    dest: path.join(projectRoot, '../frontend/public/modules/yz-audio-visualiser.js')
+    dest: path.join(frontendRoot, 'public/modules/yz-audio-visualiser.js')
   },
   {
     src: path.join(projectRoot, 'dist/audio-visualiser.d.ts'),
-    dest: path.join(projectRoot, '../frontend/src/types/yz-audio-visualiser.d.ts')
+    dest: path.join(frontendRoot, 'src/types/yz-audio-visualiser.d.ts')
   }
 ]

@@ -35,8 +38,13 @@ for (const { src, dest } of copies) {
     // Check if destination directory exists
     const destDir = path.dirname(dest)
     if (!fs.existsSync(destDir)) {
-      console.log(`ℹ️  Destination directory not found (this is normal for CI builds)`)
-      continue
+      console.log(`ℹ️  Destination directory not found: ${destDir}`)
+      // Try to create it if it's missing but we're in the right environment
+      try {
+        fs.mkdirSync(destDir, { recursive: true })
+      } catch (e) {
+        continue
+      }
     }

     // Copy the file
diff --git a/src/hooks/useVisualizerReset.ts b/src/hooks/useVisualizerReset.ts
index a1423c1..0f264b4 100644
--- a/src/hooks/useVisualizerReset.ts
+++ b/src/hooks/useVisualizerReset.ts
@@ -1,5 +1,5 @@
 import { useCallback } from 'react'
-import { useStore } from '../store'
+import { useStore, VISUALISER_STORAGE_KEY } from '../store'
 import { getVisualizerIds, getVisualizerConfig } from '../_generated'
 import { DEFAULT_ASTROFOX_CONFIG } from '../components/Visualisers'

@@ -26,7 +26,7 @@ export const useVisualizerReset = () => {
   const handleResetAll = useCallback(() => {
     if (window.confirm('Are you sure you want to reset ALL settings and visualisers to default? This cannot be undone.')) {
       // Clear Zustand persisted storage
-      localStorage.removeItem('visualiser-storage')
+      localStorage.removeItem(VISUALISER_STORAGE_KEY)
       window.location.reload()
     }
   }, [])
diff --git a/src/store/index.ts b/src/store/index.ts
index 0cdab04..20f063a 100644
--- a/src/store/index.ts
+++ b/src/store/index.ts
@@ -1,5 +1,5 @@
 // Main store export
-export { default as useStore } from './useStore'
+export { default as useStore, VISUALISER_STORAGE_KEY } from './useStore'
 export type { IStore } from './useStore'

 // Store slice types
diff --git a/src/store/queryParamInit.ts b/src/store/queryParamInit.ts
index a44181a..b34cf70 100644
--- a/src/store/queryParamInit.ts
+++ b/src/store/queryParamInit.ts
@@ -63,6 +63,7 @@ export function parseQueryParams(): {
   autoChange?: boolean
   fxEnabled?: boolean
   showFxPanel?: boolean
+  storageName?: string
   [key: string]: any
 } {
   // Extract query string - works for both standalone mode and HashRouter embedded mode
@@ -92,6 +93,11 @@ export function parseQueryParams(): {
   if (showFxPanelParam !== null) {
     result.showFxPanel = showFxPanelParam === 'true' || showFxPanelParam === '1' || showFxPanelParam === 'yes'
   }
+
+  const storageNameParam = params.get('storageName')
+  if (storageNameParam) {
+    result.storageName = storageNameParam
+  }

   const visualParam = params.get('visual')

diff --git a/src/store/useStore.ts b/src/store/useStore.ts
index f08c928..4ea0f4c 100644
--- a/src/store/useStore.ts
+++ b/src/store/useStore.ts
@@ -15,6 +15,9 @@ declare const process: { env: { NODE_ENV: string } }
 // Parse query params synchronously before store creation
 const queryParamOverrides = parseQueryParams()

+// Determine storage key from query params, global window, or default
+export const VISUALISER_STORAGE_KEY = queryParamOverrides.storageName || (window as any).VISUALISER_STORAGE_NAME || 'visualiser-storage-v6';
+
 const useStore = create(
   devtools(
     persist(
@@ -33,7 +36,7 @@ const useStore = create(
         })
       ),
       {
-        name: 'visualiser-storage-v6',
+        name: VISUALISER_STORAGE_KEY,
         version: VISUALISER_STORE_VERSION,
         partialize: (state: any) => {
           // Exclude non-serializable or static metadata from persistence
diff --git a/src/types/VisualiserIso.ts b/src/types/VisualiserIso.ts
index e36a85a..f317085 100644
--- a/src/types/VisualiserIso.ts
+++ b/src/types/VisualiserIso.ts
@@ -7,4 +7,5 @@ export interface VisualiserIsoProps {
   ConfigFormComponent?: React.ComponentType<any>
   configData?: any
   onClose?: () => void
-}
\ No newline at end of file
+  storageName?: string
+}
